resources:
  jobs:
    agents_in_a_day_setup:
      name: "Agents in a Day - Setup"

      tasks:
        - task_key: setup
          notebook_task:
            notebook_path: ../src/notebooks/Lab 0 - Setup.py
            base_parameters:
              catalog: ${var.catalog}
              prefix: ${var.prefix}

      queue:
        enabled: true
