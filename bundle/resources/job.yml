resources:
  jobs:
    agents_in_a_day_setup:
      name: "Agents in a Day — Setup"

      parameters:
        - name: catalog
          default: ${var.catalog}
        - name: prefix
          default: ${var.prefix}

      tasks:
        - task_key: setup
          notebook_task:
            notebook_path: ../labs/Lab 0 - Setup.py

      queue:
        enabled: true
